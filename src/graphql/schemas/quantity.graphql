# import decimal from 'decimal.graphql'
# import code from "code.graphql"
# import uri from "uri.graphql"

type Quantity {
    value: decimal
    comparator: code
    unit: String
    system: uri
    code: code
}
