# This file is auto-generated by generate_classes so do not edit manually
# import Appointment from "../resources/appointment.graphql"

extend type Query {
    """
    Appointment
        A booking of a healthcare event among patient(s), practitioner(s), related
    person(s) and/or device(s) for a specific date/time. This may result in one or
    more Encounter(s).
        If the element is present, it must have either a @value, an @id, or extensions
    """
    appointment(

        # some old search parameters that we misspelled in the past so have to maintain backwards compatibility
        """
        list of ids to retrieve.  DEPRECATED: Use `_id`
        """
        id: [String]

        # search parameters for all resources
        """
        _id (FHIR type: token)
        Logical id of this artifact
        """
        _id: SearchString
        """
        _lastUpdated (FHIR type: date)
        When the resource version last changed
        """
        _lastUpdated: SearchDate
        """
        _profile (FHIR type: uri)
        Profiles this resource claims to conform to
        """
        _profile: SearchString
        """
        _security (FHIR type: token)
        Security Labels applied to this resource
        """
        _security: SearchToken
        """
        _source (FHIR type: uri)
        Identifies where the resource comes from
        """
        _source: SearchString
        """
        _tag (FHIR type: token)
        Tags applied to this resource
        """
        _tag: SearchToken

        # search parameters for Appointment
        """
        actor (FHIR type: reference)
        Any one of the individuals participating in the appointment
        """
        actor: SearchReference
        """
        appointment-type (FHIR type: token)
        The style of appointment or patient that has been booked in the slot (not service type)
        """
        appointment_type: SearchToken
        """
        based-on (FHIR type: reference)
        The service request this appointment is allocated to assess
        """
        based_on: SearchReference
        """
        date (FHIR type: date)
        Appointment date/time.
        """
        date: SearchDate
        """
        identifier (FHIR type: token)
        An Identifier of the Appointment
        """
        identifier: SearchToken
        """
        location (FHIR type: reference)
        This location is listed in the participants of the appointment
        """
        location: SearchReference
        """
        part-status (FHIR type: token)
        The Participation status of the subject, or other participant on the appointment. Can be used to locate participants that have not responded to meeting requests.
        """
        part_status: SearchToken
        """
        patient (FHIR type: reference)
        One of the individuals of the appointment is this patient
        """
        patient: SearchReference
        """
        practitioner (FHIR type: reference)
        One of the individuals of the appointment is this practitioner
        """
        practitioner: SearchReference
        """
        reason-code (FHIR type: token)
        Coded reason this appointment is scheduled
        """
        reason_code: SearchToken
        """
        reason-reference (FHIR type: reference)
        Reason the appointment is to take place (resource)
        """
        reason_reference: SearchReference
        """
        service-category (FHIR type: token)
        A broad categorization of the service that is to be performed during this appointment
        """
        service_category: SearchToken
        """
        service-type (FHIR type: token)
        The specific service that is to be performed during this appointment
        """
        service_type: SearchToken
        """
        slot (FHIR type: reference)
        The slots that this appointment is filling
        """
        slot: SearchReference
        """
        specialty (FHIR type: token)
        The specialty of a practitioner that would be required to perform the service requested in this appointment
        """
        specialty: SearchToken
        """
        status (FHIR type: token)
        The overall status of the appointment
        """
        status: SearchToken
        """
        supporting-info (FHIR type: reference)
        Additional information to support the appointment
        """
        supporting_info: SearchReference

        # miscellaneous parameters
        """
        return total number of records that meet this query
        """
        _total: TotalType
        """
        sort records by these fields.  The fields can be nested fields. Prepend with "-" to indicate descending sort
            Examples: ["id", "-meta.lastUpdated"]
        """
        _sort: [String]
        """
        limit records to this count.  Default is 10
        """
        _count: Int
        """
        page number to retrieve
        """
        _getpagesoffset: Int
    ): [Appointment]
}