# This file is auto-generated by generate_classes so do not edit manually
# import Task from "../resources/task.graphql"

extend type Query {
    """
    Task
        A task to be performed.
        If the element is present, it must have either a @value, an @id, or extensions
    """
    task(

        # some old search parameters that we misspelled in the past so have to maintain backwards compatibility
        """
        list of ids to retrieve.  DEPRECATED: Use `_id`
        """
        id: [String]

        # search parameters for all resources
        """
        _id (FHIR type: token)
        Logical id of this artifact
        """
        _id: SearchString
        """
        _lastUpdated (FHIR type: date)
        When the resource version last changed
        """
        _lastUpdated: SearchDate
        """
        _profile (FHIR type: uri)
        Profiles this resource claims to conform to
        """
        _profile: SearchString
        """
        _security (FHIR type: token)
        Security Labels applied to this resource
        """
        _security: SearchToken
        """
        _source (FHIR type: uri)
        Identifies where the resource comes from
        """
        _source: SearchString
        """
        _tag (FHIR type: token)
        Tags applied to this resource
        """
        _tag: SearchToken

        # search parameters for Task
        """
        authored-on (FHIR type: date)
        Search by creation date
        """
        authored_on: SearchDate
        """
        based-on (FHIR type: reference)
        Search by requests this task is based on
        """
        based_on: SearchReference
        """
        business-status (FHIR type: token)
        Search by business status
        """
        business_status: SearchToken
        """
        code (FHIR type: token)
        Search by task code
        """
        code: SearchToken
        """
        encounter (FHIR type: reference)
        Search by encounter
        """
        encounter: SearchReference
        """
        focus (FHIR type: reference)
        Search by task focus
        """
        focus: SearchReference
        """
        group-identifier (FHIR type: token)
        Search by group identifier
        """
        group_identifier: SearchToken
        """
        identifier (FHIR type: token)
        Search for a task instance by its business identifier
        """
        identifier: SearchToken
        """
        intent (FHIR type: token)
        Search by task intent
        """
        intent: SearchToken
        """
        modified (FHIR type: date)
        Search by last modification date
        """
        modified: SearchDate
        """
        owner (FHIR type: reference)
        Search by task owner
        """
        owner: SearchReference
        """
        part-of (FHIR type: reference)
        Search by task this task is part of
        """
        part_of: SearchReference
        """
        patient (FHIR type: reference)
        Search by patient
        """
        patient: SearchReference
        """
        performer (FHIR type: token)
        Search by recommended type of performer (e.g., Requester, Performer, Scheduler).
        """
        performer: SearchToken
        """
        period (FHIR type: date)
        Search by period Task is/was underway
        """
        period: SearchDate
        """
        priority (FHIR type: token)
        Search by task priority
        """
        priority: SearchToken
        """
        requester (FHIR type: reference)
        Search by task requester
        """
        requester: SearchReference
        """
        status (FHIR type: token)
        Search by task status
        """
        status: SearchToken
        """
        subject (FHIR type: reference)
        Search by subject
        """
        subject: SearchReference

        # miscellaneous parameters
        """
        return total number of records that meet this query
        """
        _total: TotalType
        """
        sort records by these fields.  The fields can be nested fields. Prepend with "-" to indicate descending sort
            Examples: ["id", "-meta.lastUpdated"]
        """
        _sort: [String]
        """
        limit records to this count.  Default is 10
        """
        _count: Int
        """
        page number to retrieve
        """
        _getpagesoffset: Int
    ): [Task]
}