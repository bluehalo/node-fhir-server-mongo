<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('../partials/head'); %>
</head>
<body class="container">

<header>
  <%- include('../partials/header', {resources: resources}); %>
</header>

<main>
  <div class="jumbotron">
    <% resources.forEach(function(res){ %>
        <%- include('../partials/resourceHeader', {res: res}); %>

        <%- include('../partials/codeableConcept', {codeableConcepts: res.serviceType, name: "Service Type"}); %>

        <%- include('../partials/reference', {references: [res.schedule], name: "Schedule"}); %>

        <h4>Start</h4><%= res.start %>
        <h4>End</h4><%= res.end %>

        <%- include('../partials/json', {res: res}); %>
    <%})%>
  </div>
</main>

<footer>
  <%- include('../partials/footer', {url: url}); %>
</footer>

</body>
</html>
