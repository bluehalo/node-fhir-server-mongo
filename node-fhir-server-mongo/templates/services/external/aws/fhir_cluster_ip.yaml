{{ if .Values.aws }}
apiVersion: v1
kind: Service
metadata:
  name: fhir-server
# namespace should match other services in this file  
  namespace: {{ .Values.namespace }}  
spec:
  clusterIP: None
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    io.kompose.service: fhir
  type: ClusterIP
status:
  loadBalancer: {}
{{ end }}